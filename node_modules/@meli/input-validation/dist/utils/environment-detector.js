"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.isProduction = isProduction;
exports.isNotProduction = isNotProduction;
let cachedFrontendEnv = null;
let frontendEnvChecked = false;
function getFrontendEnv() {
    if (frontendEnvChecked) {
        return cachedFrontendEnv;
    }
    frontendEnvChecked = true;
    try {
        // eslint-disable-next-line @typescript-eslint/no-var-requires, n/global-require, import/no-extraneous-dependencies
        const env = require('frontend-env');
        if (env && typeof env.PRODUCTION === 'boolean') {
            cachedFrontendEnv = env;
            return env;
        }
    }
    catch {
        cachedFrontendEnv = null;
    }
    return null;
}
function isProduction() {
    const frontendEnv = getFrontendEnv();
    if (frontendEnv) {
        return Boolean(frontendEnv.PRODUCTION);
    }
    return (process.env.NODE_ENV === 'production' || process.env.NODE_ENV === 'prod');
}
function isNotProduction() {
    return !isProduction();
}
