const RESTCLIENT_PREFIX = 'toolkits.services.restclient';

module.exports = {
  POOL_NAME: `${RESTCLIENT_PREFIX}.pool_name`,
  RETRY_STRATEGY: `${RESTCLIENT_PREFIX}.retry_strategy`,
  RETRY_POLICY: `${RESTCLIENT_PREFIX}.retry_policy`,
  RETRIES: `${RESTCLIENT_PREFIX}.retries`,
  VERSION: `${RESTCLIENT_PREFIX}.version`,
  RESTCLIENT_SPAN_NAME: 'RestClient',
  LINEAR_BACKOFF: 'LinearBackoff',
  EXPONENTIAL_BACKOFF: 'ExponentialBackoff',
};
