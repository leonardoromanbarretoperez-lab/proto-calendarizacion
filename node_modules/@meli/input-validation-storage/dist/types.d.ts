export interface ModuleScopeInfo {
    moduleType: string;
    scope: string;
    modulePath: string;
    stackLines?: Array<string>;
    line?: number;
}
export interface ValidationStateStore {
    state: boolean;
    fullyValidated: boolean;
    validatedProps: Set<string>;
    scopes: Map<string, {
        validatedProps: Set<string>;
        fullyValidated?: boolean;
    }>;
}
export interface ValidationOptions {
    global?: boolean;
}
