import type { SerializeJSOptions } from 'serialize-javascript';

import serialize from 'serialize-javascript';

const SERIALIZE_OPTIONS: SerializeJSOptions = { isJSON: true };

export function serializeJSON(data: object | string) {
  return serialize(data, SERIALIZE_OPTIONS);
}

export { type SerializeJSOptions, serialize };
