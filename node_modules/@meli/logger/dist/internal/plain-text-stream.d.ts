import build from 'pino-abstract-transport';
type PinoLog = Record<string, unknown> & {
    time: string;
    level: string;
    msg: string;
};
/**
 * Convert a tags object to the `[key:value] [key:value]` format.
 */
export declare function json2tags(tags: Record<string, unknown>): string;
/**
 * Build a plain-text log line from a parsed pino log object.
 * Format: `timestamp - level: msg - [tag:value]... [level:LEVEL]`
 */
export declare function buildLine(pinoLog: PinoLog): string;
/**
 * Creates a stream that formats pino log objects as plain text and writes them to stdout.
 * Uses pino-abstract-transport to receive pre-parsed objects, avoiding JSON.parse overhead.
 */
export declare function createPlainTextStream(): import("stream").Transform & build.OnUnknown;
export {};
