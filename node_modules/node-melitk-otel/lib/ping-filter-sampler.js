"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.PingFilterSampler = void 0;
const sdk_trace_node_1 = require("@opentelemetry/sdk-trace-node");
const semantic_conventions_1 = require("@opentelemetry/semantic-conventions");
const pingFilterSamplerDescription = 'PingFilterSampler';
const pingEndpoint = '/ping';
const pingHTTPRouteAttribute = {
    key: semantic_conventions_1.ATTR_HTTP_ROUTE,
    value: pingEndpoint,
};
const pingHTTPTargetAttribute = {
    key: semantic_conventions_1.SEMATTRS_HTTP_TARGET,
    value: pingEndpoint,
};
/**
 * This sampler will drop all spans which name matches a ping endpoint route
 */
class PingFilterSampler {
    rootSampler;
    pingSpanName;
    // Should be composed with other Samplers
    constructor(rootSampler) {
        this.rootSampler = rootSampler;
        this.pingSpanName = undefined;
    }
    /**
     * ShouldSample will drop spans that match the ping route.
     * Once a ping span is found that ping span name is saved to avoid iterating through the span parameters every time.
     */
    shouldSample(context, traceId, spanName, spanKind, attributes, links) {
        if (this.pingSpanName !== '' && this.pingSpanName === spanName) {
            return { decision: sdk_trace_node_1.SamplingDecision.NOT_RECORD /* DROP */ };
        }
        const shouldDrop = Object.entries(attributes).some(([key, value]) => {
            if ((key === pingHTTPRouteAttribute.key ||
                key === pingHTTPTargetAttribute.key) &&
                value === pingEndpoint) {
                this.pingSpanName = spanName;
                return true;
            }
            return false;
        });
        if (shouldDrop) {
            return { decision: sdk_trace_node_1.SamplingDecision.NOT_RECORD /* DROP */ };
        }
        return this.rootSampler.shouldSample(context, traceId, spanName, spanKind, attributes, links);
    }
    /**
     * Description returns the sampler composition
     */
    toString() {
        return `${pingFilterSamplerDescription}{${this.rootSampler.toString()}}`;
    }
}
exports.PingFilterSampler = PingFilterSampler;
