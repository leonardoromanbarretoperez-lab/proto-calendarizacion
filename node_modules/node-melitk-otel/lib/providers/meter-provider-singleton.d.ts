import { MeterProvider } from '@opentelemetry/sdk-metrics';
export declare function getMetricAllowKeys(envKey: string, defaults: Array<string>): Array<string>;
export declare class MeterProviderSingleton {
    private static instance;
    static getInstance(): MeterProvider;
    private static newMeterProvider;
    private static createMetricViews;
}
