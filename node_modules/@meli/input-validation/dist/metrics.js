"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.increment = void 0;
const statsd_1 = require("@meli/statsd");
const environment_detector_1 = require("./utils/environment-detector");
const statsd = new statsd_1.MeliStatsD();
const getMetricsImplementation = () => {
    if ((0, environment_detector_1.isProduction)() &&
        typeof window === 'undefined' &&
        typeof process !== 'undefined' &&
        typeof global !== 'undefined') {
        try {
            if (typeof statsd.increment === 'function') {
                const safeIncrement = (...args) => {
                    try {
                        // @ts-expect-error: statsd.increment may accept any arguments
                        return statsd.increment(...args);
                    }
                    catch (e) {
                        console.warn('Error calling statsd.increment:', e);
                        return undefined;
                    }
                };
                return {
                    increment: safeIncrement,
                };
            }
        }
        catch (e) {
            console.warn('Error loading frontend-statsd/internal, using noop functions', e);
        }
    }
    return {
        // eslint-disable-next-line @typescript-eslint/no-unused-vars
        increment: (..._args) => { },
    };
};
const increment = (...args) => {
    const { increment: incrementImpl } = getMetricsImplementation();
    return incrementImpl(...args);
};
exports.increment = increment;
