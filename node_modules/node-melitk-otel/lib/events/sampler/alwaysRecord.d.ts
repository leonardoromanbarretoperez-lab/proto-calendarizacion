import type { Sampler, SamplingResult } from '@opentelemetry/sdk-trace-node';
import type { Activator } from '../activator/types';
import type { Attributes, Context, Link, SpanKind } from '@opentelemetry/api';
import type SamplerTelemetry from './telemetry';
/**
 * AlwaysRecordSampler that decides whether a span should be sampled or not based on Events activation.
 * @implements {Sampler}
 * @class
 */
export declare class AlwaysRecordSampler implements Sampler {
    private readonly rootSampler;
    private readonly pingSampler;
    private readonly activator;
    private readonly telemetry;
    constructor(rootSampler: Sampler, pingSampler: Sampler, activator: Activator, telemetry: SamplerTelemetry);
    /**
     * ShouldSample will decide whether a span should be exported based on the activator result.
     */
    shouldSample(context: Context, traceId: string, spanName: string, spanKind: SpanKind, attributes: Attributes, links: Array<Link>): SamplingResult;
    toString(): string;
}
export declare function NewAlwaysRecordSampler(rootSampler: Sampler, pingSampler: Sampler, activator: Activator, telemetry: SamplerTelemetry): AlwaysRecordSampler;
