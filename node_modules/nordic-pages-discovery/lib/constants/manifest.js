const path = require('path');

const NORDIC_DIR_PATH = path.resolve('.nordic');
const DISCOVERY_MANIFEST_FILE_PATH = path.join(
  NORDIC_DIR_PATH,
  '/discovery/manifest.json',
);
const ISLANDS_MANIFEST_FILE_PATH = path.join(
  NORDIC_DIR_PATH,
  '/discovery/islands-manifest.json',
);

module.exports = {
  NORDIC_DIR_PATH,
  DISCOVERY_MANIFEST_FILE_PATH,
  ISLANDS_MANIFEST_FILE_PATH,
};
