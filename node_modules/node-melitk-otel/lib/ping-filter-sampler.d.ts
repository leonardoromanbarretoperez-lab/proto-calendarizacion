import type { Sampler, SamplingResult } from '@opentelemetry/sdk-trace-node';
import type { Attributes, Context, Link, SpanKind } from '@opentelemetry/api';
/**
 * This sampler will drop all spans which name matches a ping endpoint route
 */
export declare class PingFilterSampler implements Sampler {
    private readonly rootSampler;
    private pingSpanName;
    constructor(rootSampler: Sampler);
    /**
     * ShouldSample will drop spans that match the ping route.
     * Once a ping span is found that ping span name is saved to avoid iterating through the span parameters every time.
     */
    shouldSample(context: Context, traceId: string, spanName: string, spanKind: SpanKind, attributes: Attributes, links: Array<Link>): SamplingResult;
    /**
     * Description returns the sampler composition
     */
    toString(): string;
}
