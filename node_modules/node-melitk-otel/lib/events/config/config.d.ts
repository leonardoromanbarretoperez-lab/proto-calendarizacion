import type { Attributes } from '@opentelemetry/api';
export interface O11YDefaults {
    o11yEvents: O11YEventsConfig;
}
export interface O11YEventsConfig {
    enabled: string;
    debugMode: string;
    otel: OTELConfig;
    activator: ActivatorConfig;
}
export interface OTELConfig {
    exporter: ExporterConfig;
    processor: ProcessorConfig;
    attributes: AttributesConfig;
}
export interface ExporterConfig {
    otlp: OTLPConfig;
}
export interface OTLPConfig {
    endpoint: string;
    exportTimeout: string;
    retry: OTLPRetryConfig;
}
export interface OTLPRetryConfig {
    enabled: string;
    initialInterval: string;
    maxInterval: string;
    maxElapsedTime: string;
}
export interface ProcessorConfig {
    maxQueueSize: string;
    scheduleDelay: string;
    exportTimeout: string;
    maxExportBatchSize: string;
    blockOnQueueFull: string;
}
export interface AttributesConfig {
    serviceName: string;
    serviceVersion: string;
    furyApplication: string;
    furyService: string;
    furyCriticality: string;
}
export interface ActivatorConfig {
    period: string;
    timeout: string;
    maxRetries: string;
    url: string;
    servicesEndpoint: string;
}
export declare const metricDefaultAttributes: Attributes;
/**
 * Loads the configs for the o11y events.
 * @returns {O11YDefaults} The default configs for the o11y events.
 */
export declare const loadConfigs: () => O11YDefaults;
