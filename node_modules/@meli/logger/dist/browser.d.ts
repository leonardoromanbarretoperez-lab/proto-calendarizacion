import type { Logger as MeliLogger, LogLevel, LoggerOptions, Tags } from './Logger';
export type { LogLevel, LoggerOptions, Tags };
interface LoggerInstance extends MeliLogger {
    name: string;
}
type LoggerConstructor = new (name: string, options?: LoggerOptions) => LoggerInstance;
export declare const Logger: LoggerConstructor;
