/* istanbul ignore file */
class OpenTelemetryDumbClient {
  spanConfig() {
    return { attributes: {} };
  }

  async asyncBuildActiveSpan(asyncCallback) {
    const response = await asyncCallback();
    return { response };
  }

  recordSpanError() {}

  addAttributesToSpan() {}

  endSpan() {}
}

module.exports = OpenTelemetryDumbClient;
