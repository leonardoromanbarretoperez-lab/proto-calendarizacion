import type { Request, Response } from 'express';
import { AsyncLocalStorage } from 'node:async_hooks';
export type ServerContextStorage = {
    req: Request;
    res: Response;
};
export declare function initStorage(): AsyncLocalStorage<ServerContextStorage>;
export declare function getContextStore(): ServerContextStorage | undefined;
