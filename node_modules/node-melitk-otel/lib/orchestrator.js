"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.orchestrateTracerProvider = void 0;
const sdk_trace_node_1 = require("@opentelemetry/sdk-trace-node");
const api_1 = require("@opentelemetry/api");
const ping_filter_sampler_1 = require("./ping-filter-sampler");
const trace_sampler_1 = require("./trace-sampler");
const tracing_1 = require("./events/tracing");
const configs_1 = require("./configs");
const package_json_1 = __importDefault(require("../package.json"));
const getBatchSpanProcessor = (exp) => new sdk_trace_node_1.BatchSpanProcessor(exp, {
    exportTimeoutMillis: configs_1.OTEL_AGENT_DEFAULT_TIMEOUT,
});
/**
 * The orchestrateTracerProvider orchestrates, or composes, what combinations of
 * samplers and tracer provider options will be instrumented according to the
 * environment of the host application.
 *
 * Sampling estrategies:
 *  1. default: parent based never sample
 *  2. jobs services: parent based probabilistic
 *  3. events: always sample encapsulating one of the other two options
 * @param {OTLPTraceExporter} exp The OTLP trace exporter.
 * @returns {NodeTracerProvider} The tracer provider.
 */
const orchestrateTracerProvider = (exp) => {
    api_1.diag.info(`${configs_1.O11Y_EVENTS_LOG_PREFIX}Extension orchestrator ${package_json_1.default.version} initializing...`);
    const eventsTracing = (0, tracing_1.newO11YEventsTracing)();
    const spanProcessors = Array(getBatchSpanProcessor(exp));
    const pingSampler = new ping_filter_sampler_1.PingFilterSampler(new sdk_trace_node_1.AlwaysOnSampler());
    const defaultSampler = (0, trace_sampler_1.getSampler)();
    api_1.diag.info(`${configs_1.O11Y_EVENTS_LOG_PREFIX}Enabled, initializing components`);
    const eventsSampler = (0, tracing_1.newSampler)(eventsTracing, defaultSampler, pingSampler);
    spanProcessors.push(...(0, tracing_1.newSpanProcessors)(eventsTracing));
    const eventsResourceBuilder = (0, tracing_1.newEventsResourceBuilder)(eventsTracing);
    const tracerProvider = new sdk_trace_node_1.NodeTracerProvider({
        sampler: eventsSampler,
        resource: eventsResourceBuilder,
        spanProcessors,
    });
    return tracerProvider;
};
exports.orchestrateTracerProvider = orchestrateTracerProvider;
