"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.setupGlobalTags = setupGlobalTags;
exports.contextProvider = contextProvider;
const server_context_1 = require("@meli/server-context");
const frontend_env_1 = __importDefault(require("frontend-env"));
const logger_error_1 = require("./logger-error");
let globalTagsMapper;
/**
 * Set a global function that maps the current request to extra log tags.
 * Multiple calls overwrite the previous mapper and emit a warning.
 */
function setupGlobalTags(mapper) {
    if (typeof mapper !== 'function') {
        throw new logger_error_1.LoggerError('setupGlobalTags expects a function');
    }
    if (globalTagsMapper && !frontend_env_1.default.PRODUCTION) {
        // eslint-disable-next-line no-console
        console.warn('[@meli/logger] setupGlobalTags was called more than once. Previous mapper will be overwritten.');
    }
    globalTagsMapper = mapper;
}
/**
 * Pino mixin: returns tags derived from the current request via globalTagsMapper.
 * Called by pino on every log call.
 */
function contextProvider() {
    if (!globalTagsMapper) {
        return {};
    }
    let req;
    try {
        req = (0, server_context_1.getReq)();
    }
    catch {
        // @meli/server-context was never setup
    }
    try {
        // eslint-disable-next-line @typescript-eslint/no-unnecessary-condition -- mapper is user code, can return nullish
        return globalTagsMapper(req) ?? {};
    }
    catch (err) {
        if (!frontend_env_1.default.PRODUCTION) {
            throw new logger_error_1.LoggerError(`globalTagsMapper threw an error: ${err instanceof Error ? err.message : String(err)}`, { cause: err });
        }
        globalTagsMapper = undefined;
        return {};
    }
}
