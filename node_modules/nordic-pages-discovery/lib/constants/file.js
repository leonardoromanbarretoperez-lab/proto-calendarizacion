const APP_FILE_MEMBERS = Object.freeze({
  COMPONENT: 'UI component',
  ISLAND: 'island',
  PAGE: 'page',
});

const FILE_OWNERS = Object.freeze({
  baseHooks: 'baseHooks',
  httpErrorPage: 'httpErrorPage',
  navigablePage: 'navigablePage',
  routerConfig: 'routerConfig',
});

const HOOKS_FILE_IDENTIFIER = {
  VIEW_SPECIFIC: 'hooks.server',
  BASE: '_hooks.server',
};

const SCRIPT_EXTENSIONS = ['tsx', 'ts', 'jsx', 'js'];
const SCRIPT_EQUIVALENT_EXTENSIONS = {
  '.tsx': ['.jsx', '.js'],
  '.jsx': ['.jsx', '.js'],
  '.ts': ['.js'],
  '.js': ['.js'],
};

const STYLE_EXTENSIONS = ['scss', 'css'];

const SCRIPT_OPTIONS = SCRIPT_EXTENSIONS.join(',');
const STYLES_OPTIONS = STYLE_EXTENSIONS.join(',');

const DEFAULT_EXPORT_KEY = 'default';

module.exports = {
  APP_FILE_MEMBERS,
  FILE_OWNERS,
  HOOKS_FILE_IDENTIFIER,
  SCRIPT_EXTENSIONS,
  STYLE_EXTENSIONS,
  SCRIPT_OPTIONS,
  STYLES_OPTIONS,
  SCRIPT_EQUIVALENT_EXTENSIONS,
  DEFAULT_EXPORT_KEY,
};
