"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const ivObject_1 = require("../../entity/ivObject");
const normalizeToZodSchemas = (schemas) => {
    if (schemas.length < 2) {
        throw new Error('At least two schemas are required');
    }
    return schemas.map((schema) => schema instanceof ivObject_1.IvObject ? schema.getSchema() : schema);
};
exports.default = normalizeToZodSchemas;
