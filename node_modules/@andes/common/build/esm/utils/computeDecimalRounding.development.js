/**
 * Computes the rounding of a number to the specified decimal places.
 *
 * @param {number} value - The number to be rounded.
 * @param {number} [decimals] - The number of decimal places to round to.
 * @returns {number} - The rounded number.
 */
const computeDecimalRounding = function (value) {
  let decimals = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : 0;
  const factor = 10 ** decimals;
  return Math.round(value * factor) / factor;
};
export { computeDecimalRounding as default };
//# sourceMappingURL=computeDecimalRounding.development.js.map
