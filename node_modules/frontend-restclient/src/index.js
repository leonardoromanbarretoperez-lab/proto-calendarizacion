/**
 * Module dependencies
 */
const RestClient = require('./RestClient');
const buildContext = require('./build-context');
const buildFlowStarterContext = require('./build-flow-context');
const retryPolicies = require('./policies');

const InitializeRestClient = (config) => new RestClient(config);

/**
 * Expose restclient factory
 */
module.exports = InitializeRestClient;
module.exports.RestClient = InitializeRestClient;
module.exports.buildContext = buildContext;
module.exports.buildFlowStarterContext = buildFlowStarterContext;
module.exports.retryPolicies = retryPolicies;
