import { createCountriesLocales, createTranslations } from './data.mjs';

class I18nCache {
    #countriesLocales = new Map();
    #translations = new Map();
    initialize(srcPath) {
        this.#countriesLocales = createCountriesLocales();
        this.#translations = createTranslations(srcPath);
    }
    getLocale(countryId) {
        return this.#countriesLocales.get(countryId);
    }
    getTranslations(locale) {
        const translations = this.#translations.get(locale);
        if (translations) {
            return translations;
        }
        const [language] = locale.split('-');
        return this.#translations.get(language) ?? {};
    }
}
const i18nCache = new I18nCache();

export { i18nCache };
