import type { MetricOptions, Counter, ObservableGauge, Histogram } from '@opentelemetry/api';
/**
 * Represents a base class for O11yMetrics.
 * @class
 * @abstract
 */
export declare abstract class O11yMetrics {
    private readonly _meter;
    /**
     * Creates a new instance of O11yMetrics.
     * @constructor Creates a new instance of O11yMetrics, using the OpenTelemetry metrics.
     * @protected
     */
    constructor();
    /**
     * Creates a new counter metric.
     * @param {string} name - The name of the metric. A {@link METRIC_PREFIX} `o11y.events.otel.sdk.` will be added on it.
     * @param {MetricOptions} options - The {@link MetricOptions} to configure the metric
     * @returns {Counter} A new {@link Counter} metric
     * @protected
     */
    newCounter(name: string, options?: MetricOptions): Counter;
    /**
     * Creates a new observable gauge metric.
     * @param {string} name - The name of the metric. A {@link METRIC_PREFIX} `o11y.events.otel.sdk.` will be added on it.
     * @param {MetricOptions} options - The {@link MetricOptions} to configure the metric
     * @returns {ObservableGauge} A new {@link ObservableGauge} gauge metric
     * @protected
     */
    newObservableGauge(name: string, options?: MetricOptions): ObservableGauge;
    /**
     * Creates a new histogram metric.
     * @param name - The name of the metric. A {@link METRIC_PREFIX} `o11y.events.otel.sdk.` will be added on it.
     * @param options - The {@link MetricOptions} to configure the metric
     * @returns {Histogram} A new {@link Histogram} metric
     * @protected
     */
    newHistogram(name: string, options?: MetricOptions): Histogram;
}
