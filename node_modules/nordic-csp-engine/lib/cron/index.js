/* istanbul ignore file */
const { initCronJobs } = require('./adapter');
const cspConfig = require('../csp-config');
const securityConfig = require('../security-config');

const JOB_CONFIGS = [cspConfig, securityConfig];

initCronJobs(JOB_CONFIGS);
