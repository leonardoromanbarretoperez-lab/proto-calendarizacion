'use strict';

var ExecutionEnvironment = require('exenv');
function _interopDefaultLegacy(e) {
  return e && typeof e === 'object' && 'default' in e ? e : {
    'default': e
  };
}
var ExecutionEnvironment__default = /*#__PURE__*/_interopDefaultLegacy(ExecutionEnvironment);

// eslint-disable-next-line security/detect-unsafe-regex
const NATIVE_APP = /^(MercadoLibre|MercadoPago)-(iOS|Android)(?:\/|%2F)((?:\d+\.){2}\d+)/;
const getNativeApp = () => {
  var _window;
  if (!ExecutionEnvironment__default["default"].canUseDOM) return;
  const userAgent = (_window = window) === null || _window === void 0 || (_window = _window.navigator) === null || _window === void 0 ? void 0 : _window.userAgent;
  const nativeAppArray = NATIVE_APP.exec(userAgent);
  if (!Array.isArray(nativeAppArray)) return;
  return {
    name: nativeAppArray[1],
    os: nativeAppArray[2],
    version: nativeAppArray[3]
  };
};
module.exports = getNativeApp;
//# sourceMappingURL=getNativeApp.development.js.map
