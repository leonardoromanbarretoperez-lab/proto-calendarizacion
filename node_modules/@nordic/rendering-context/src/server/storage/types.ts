import type { NordicRenderingContext } from '../../common/types';
import type { CriticalCssStore } from '../critical-css/store';
import type { MetricsStore } from '../metrics/store/types';
import type { LayoutViewStore } from '../layout/types';
import type { IIFEScriptName } from '../iife/store';

export type {
  LayoutViewStore,
  LayoutViewHeadSlots,
  LayoutViewBodySlots,
  LayoutViewSlotData,
  LayoutViewSlotProps,
} from '../layout/types';

export type NordicRenderingServerContext = NordicRenderingContext & {
  criticalCss: CriticalCssStore;
  metrics: MetricsStore;
  iifeScripts: Map<IIFEScriptName, string>;
  melidata?: unknown;
  islands?: unknown;
  layoutView: LayoutViewStore;
};
