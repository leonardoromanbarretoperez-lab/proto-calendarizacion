import base32Encode from 'base32-encode';
const encodeStringToBase32 = inputString => {
  if (typeof inputString !== 'string') return '';
  const encoder = new TextEncoder();
  const buffer = encoder.encode(inputString);
  const encodedData = base32Encode(buffer, 'RFC3548');
  return encodedData;
};
var encodeBase32 = {
  encodeStringToBase32
};
export { encodeBase32 as default };
