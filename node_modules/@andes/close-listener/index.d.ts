import type { ComponentClass, ReactElement } from 'react';

export type CloseListenerProps = {
  children: ReactElement;
  handleClick?: boolean;
  handleEsc?: boolean;
  onCloseOutside: (event: KeyboardEvent | MouseEvent) => void;
};

export const CloseListener: ComponentClass<CloseListenerProps>;
