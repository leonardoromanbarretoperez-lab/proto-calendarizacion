import { getContext } from './storage';

export function setLayoutMiddlewareOptions<T = unknown>(
  layoutMiddlewareOptions: T,
) {
  getContext().layoutMiddlewareOptions = layoutMiddlewareOptions;
}
