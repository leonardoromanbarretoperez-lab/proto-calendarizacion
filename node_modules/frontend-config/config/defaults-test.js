const productionConfig = require('./defaults');
const { deepMerge } = require('../src/utils');

const config = {
  ML: {
    MLA: {
      defaultTitle: 'Mercado Libre Argentina - Donde comprar y vender de todo!',
    },
  },
};

module.exports = deepMerge({}, productionConfig, config);
