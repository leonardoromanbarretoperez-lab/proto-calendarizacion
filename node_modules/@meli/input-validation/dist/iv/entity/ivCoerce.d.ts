import { z } from 'zod';
type ZodCoerce = typeof z.coerce;
/**
 * Shape of `iv.coerce`: same as `z.coerce` but with `boolean` marked `@deprecated`.
 *
 * The deprecation is a type-level overlay — runtime behavior is preserved for
 * backwards compatibility, but IDEs will surface a warning when `coerce.boolean()`
 * is used, pointing users to `iv.stringBoolean()`.
 */
export type IvCoerce = Omit<ZodCoerce, 'boolean'> & {
    /**
     * @deprecated `coerce.boolean()` delegates to Zod's `z.coerce.boolean()`, which uses
     * JavaScript's `Boolean()` function. Any non-empty string — including `'false'` —
     * coerces to `true`. This makes it semantically incorrect for HTTP query/path params:
     *
     * ```ts
     * iv.coerce.boolean().parse('false'); // → true  ✗
     * iv.coerce.boolean().parse('sssss'); // → true  ✗
     * ```
     *
     * Use `iv.stringBoolean()` instead:
     * - `'true'`  → `true`
     * - `'false'` → `false`
     * - anything else → validation error
     */
    boolean: ZodCoerce['boolean'];
};
declare const ivCoerce: IvCoerce;
export default ivCoerce;
