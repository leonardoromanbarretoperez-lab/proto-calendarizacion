/* eslint-disable global-require */
module.exports = {
  /** @type {typeof import('./OpenTelemetryClient')} */
  OpenTelemetryClient:
    process.env.OTEL_AGENT_ENABLED === 'true'
      ? require('./OpenTelemetryClient')
      : require('./OpenTelemetryDumbClient'),
};
