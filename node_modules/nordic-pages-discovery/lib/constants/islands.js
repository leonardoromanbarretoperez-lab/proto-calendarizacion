const COMPONENT_TYPES = {
  ISLAND_COMPONENT: 'ISLAND',
  UI_COMPONENT: 'UI COMPONENT',
};

const IMPORT_TYPES = Object.freeze({
  NORDIC_ISLAND: 'NORDIC_ISLAND',
  NORDIC_LAZY: 'NORDIC_LAZY',
  NORDIC_ALIAS: 'NORDIC_ALIAS',
  RELATIVE: 'RELATIVE',
  NODE_SUBPATH: 'NODE_SUBPATH',
  DEFAULT: 'default',
});

const NORDIC_ISLAND_ENTRYPOINT = 'nordic/islands';

const ISLAND_WRAPPER = 'Island';

module.exports = {
  COMPONENT_TYPES,
  NORDIC_ISLAND_ENTRYPOINT,
  IMPORT_TYPES,
  ISLAND_WRAPPER,
};
