import * as express from 'express';
import * as qs from 'qs';
import * as express_serve_static_core from 'express-serve-static-core';
import { I18nMiddlewareConfig } from '@meli/i18n/middleware';
import * as react from 'react';
import { HTMLElementType, ReactElement } from 'react';
import { I18n as I18n$1, I18nOptions } from '@meli/i18n';

type NordicI18nMiddlewareConfig = Partial<Omit<I18nMiddlewareConfig, 'I18nClass'>>;
/**
 * Creates an I18n instance per request, defining properties: `req.translations` and `req.i18n`.
 *
 * @see {@link https://nordic.adminml.com/docs/api/Nordic%20Modules/nordic-i18n#middlewares | Documentation}
 *
 * @example
 *
 * import ragnar from 'nordic/ragnar';
 * import { createNordicI18nMiddlewares } from 'nordic/i18n/middleware';
 *
 * const appRouter = router();
 *
 * appRouter.use(createNordicI18nMiddlewares());
 *
 */
declare function createNordicI18nMiddlewares(config?: NordicI18nMiddlewareConfig): express.RequestHandler<express_serve_static_core.ParamsDictionary, any, any, qs.ParsedQs, Record<string, any>>[];
declare module 'express-serve-static-core' {
    interface Request {
        i18n?: I18n;
    }
}

type NordicI18nConfig = NordicI18nMiddlewareConfig & {
    /**
     * Name of the repository on GitHub.
     *
     * For more info refer to the [docs](https://nordic.adminml.com/docs/api/Nordic%20Modules/frontend-i18n#configuration).
     *
     * @example 'fury_fbm-panel-frontend'
     */
    app: string;
    /**
     * Tag name to recognize the project inside Crowdin. **Avoid using rare characters**.
     *
     * For more info refer to the [docs](https://nordic.adminml.com/docs/api/Nordic%20Modules/frontend-i18n#configuration).
     *
     * @example 'fbmfulfillment'
     */
    project?: string;
    /**
     * Name of translation project in Crowdin.
     *
     * Actual apps that are already using the Babel service have a `translationProject: 'meli'` by default, so they don't need to specify this property.
     * **For new apps ask a CM team for the name**.
     *
     * For more info refer to the [docs](https://nordic.adminml.com/docs/api/Nordic%20Modules/frontend-i18n#configuration).
     *
     * @example 'fbm'
     */
    translationProject?: string;
    /**
     * File extensions that will be used to scan the terms.
     *
     * For more info refer to the [docs](https://nordic.adminml.com/docs/api/Nordic%20Modules/frontend-i18n#configuration).
     *
     * @default ['js']
     * @example ['js', 'jsx', 'ts', 'tsx']
     */
    extNames?: Array<string>;
};
declare global {
    namespace Nordic {
        interface Config {
            /**
             * i18n CLI config.
             *
             * For more info refer to the [docs](https://nordic.adminml.com/docs/api/Nordic%20Modules/frontend-i18n#cli).
             */
            i18n?: NordicI18nConfig;
        }
    }
}
type IsEqual<A, B> = (<G>() => G extends G | (A & G) ? 1 : 2) extends <G>() => G extends G | (B & G) ? 1 : 2 ? true : false;
type Filter<KeyType, ExcludeType> = IsEqual<KeyType, ExcludeType> extends true ? never : KeyType extends ExcludeType ? never : KeyType;
/**
 * Based on type-fest's `Except` type.
 * [Link to implementation](https://github.com/sindresorhus/type-fest/blob/main/source/except.d.ts)
 */
type Except<ObjectType, KeysType extends keyof ObjectType> = {
    [KeyType in keyof ObjectType as Filter<KeyType, KeysType>]: ObjectType[KeyType];
};
type Without<FirstType, SecondType> = {
    [KeyType in Exclude<keyof FirstType, keyof SecondType>]?: never;
};
/**
 * Based on type-fest's `MergeExclusive` type.
 * [Link to implementation](https://github.com/sindresorhus/type-fest/blob/main/source/merge-exclusive.d.ts)
 */
type MergeExclusive<FirstType, SecondType> = FirstType | SecondType extends object ? (FirstType & Without<SecondType, FirstType>) | (SecondType & Without<FirstType, SecondType>) : FirstType | SecondType;

type PrimitiveType = boolean | number | string;
type I18nBaseProps = {
    text: string;
    /**
     * Wrap the rendered text in a custom HTML element.
     *
     * When using `values` or `components` props, by default no wrapper is used, returning an array of React elements. When using the `tags` prop, wrapper is a `div` element by default.
     *
     * @see {@link https://nordic.adminml.com/docs/api/Nordic%20Modules/nordic-i18n#settings | Documentation}
     * @example
     * i18n.jsx.gettext('My awesome wording', { wrapper: 'p' })
     * // <p>My awesome wording</p>
     */
    wrapper?: HTMLElementType;
    /**
     * Add a custom class name to the wrapper element. Requires `wrapper` to be set.
     *
     * @see {@link https://nordic.adminml.com/docs/api/Nordic%20Modules/nordic-i18n#settings | Documentation}
     * @example
     * i18n.jsx.gettext('My awesome wording', { wrapper: 'p', className: 'my-class' })
     * // <p class="my-class">My awesome wording</p>
     */
    className?: string;
};

type I18nValues = Record<string, PrimitiveType | ReactElement | (() => ReactElement) | ((chunks: string) => ReactElement)>;
type I18nWithValuesProps = I18nBaseProps & {
    /**
     * Replace placeholders with given values or React elements.
     *
     * @see {@link https://nordic.adminml.com/docs/api/Nordic%20Modules/nordic-i18n#settingsvalues | Documentation}
     * @example
     * i18n.jsx.gettext('Hello {name}! Click this <link>link</link>.', {
     *   values: {
     *     name: 'John',
     *     link: (chunks) => <a href="https://www.google.com">{chunks}</a>
     *   }
     * })
     * // Hello John! Click this <a href="https://www.google.com">link</a>.
     *
     * @example
     * i18n.jsx.ngettext(
     *   'Hello {name}! You have {count} message {icon}.',
     *   'Hello {name}! You have {count} messages {icon}.',
     *   {
     *     quantity: 10,
     *     values: { name: 'John', count: 10, icon: <span>✉️</span> }
     * })
     * // Hello John! You have 10 messages <span>✉️</span>.
     */
    values: I18nValues;
};

type I18nWithTagsProps = I18nBaseProps & {
    /**
     * HTML tags to replace the placeholders in the given text.
     *
     * @deprecated Use `values` prop instead.
     * @see {@link https://nordic.adminml.com/docs/migration-guides/modules/i18n/tags-and-components-to-values | Migration Guide}
     */
    tags: Record<number, string>;
    /**
     * Replace placeholders with given values.
     *
     * @deprecated Use `values` prop instead.
     * @see {@link https://nordic.adminml.com/docs/migration-guides/modules/i18n/tags-and-components-to-values | Migration Guide}
     */
    replacements?: Array<PrimitiveType> | Record<number, PrimitiveType>;
};

type I18nReplacementComponents = (() => ReactElement) | ((text: string) => ReactElement);
type Replacements = Array<PrimitiveType> | Record<number, PrimitiveType>;
type I18nWithComponentsProps = I18nBaseProps & {
    /**
     * Replace placeholders with custom React components.
     *
     * @deprecated Use `values` prop instead.
     * @see {@link https://nordic.adminml.com/docs/migration-guides/modules/i18n/tags-and-components-to-values | Migration Guide}
     */
    components: Record<number, I18nReplacementComponents>;
    /**
     * Replace placeholders with given values.
     *
     * @deprecated Use `values` prop instead.
     * @see {@link https://nordic.adminml.com/docs/migration-guides/modules/i18n/tags-and-components-to-values | Migration Guide}
     */
    replacements?: Replacements;
};

type I18nProps = MergeExclusive<MergeExclusive<I18nWithTagsProps, I18nWithComponentsProps>, I18nWithValuesProps>;

type I18nJsxSettings = Except<I18nProps, 'text'>;
type I18nJsxPluralSettings = I18nJsxSettings & {
    /**
     * Number to determine if singular or plural wording should be used.
     *
     * @see {@link https://nordic.adminml.com/docs/api/Nordic%20Modules/nordic-i18n#i18njsxngettext | Documentation}
     */
    quantity: number;
};

declare class I18n extends I18n$1 {
    #private;
    jsx: {
        /**
         * Given a text and settings, returns a translated JSX element.
         *
         * @see {@link https://nordic.adminml.com/docs/api/Nordic%20Modules/nordic-i18n#i18njsxgettext | Documentation}
         */
        gettext: (text: string, settings: I18nJsxSettings) => react.JSX.Element;
        /**
         * Given a text in singular, a text in plural form and settings with a quantity, returns a translated JSX element.
         *
         * @see {@link https://nordic.adminml.com/docs/api/Nordic%20Modules/nordic-i18n#i18njsxngettext | Documentation}
         */
        ngettext: (singularText: string, pluralText: string, settings: I18nJsxPluralSettings) => react.JSX.Element;
        /**
         * Given a text and a context, returns a translated JSX element.
         *
         * @see {@link https://nordic.adminml.com/docs/api/Nordic%20Modules/nordic-i18n#i18njsxpgettext | Documentation}
         */
        pgettext: (context: string, text: string, settings: I18nJsxSettings) => react.JSX.Element;
        /**
         * Given a text in singular and plural form, settings with a quantity and context, returns a translated JSX element.
         *
         * @see {@link https://nordic.adminml.com/docs/api/Nordic%20Modules/nordic-i18n#i18njsxnpgettext | Documentation}
         */
        npgettext: (context: string, singularText: string, pluralText: string, settings: I18nJsxPluralSettings) => react.JSX.Element;
    };
    toJSON(): I18nOptions;
}

export { I18n as I, createNordicI18nMiddlewares as d };
export type { NordicI18nConfig as N, I18nJsxPluralSettings as a, I18nJsxSettings as b, NordicI18nMiddlewareConfig as c };
