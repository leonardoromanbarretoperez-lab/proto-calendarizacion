'use strict';

var middleware = require('@meli/i18n/middleware');
var frontendConfig = require('frontend-config');
var i18n = require('./i18n-Ba0c-E4l.js');
var coreContext = require('@nordic/core-context');
var iv = require('@meli/input-validation');
var resolveAcceptLanguage = require('resolve-accept-language');
var frontendCountry_config = require('frontend-country_config');
require('react/jsx-runtime');
require('@meli/i18n');
require('react');
require('@meli/deprecation');
require('@nordic/utils/error');

function _interopNamespaceDefault(e) {
    var n = Object.create(null);
    if (e) {
        Object.keys(e).forEach(function (k) {
            if (k !== 'default') {
                var d = Object.getOwnPropertyDescriptor(e, k);
                Object.defineProperty(n, k, d.get ? d : {
                    enumerable: true,
                    get: function () { return e[k]; }
                });
            }
        });
    }
    n.default = e;
    return Object.freeze(n);
}

var iv__namespace = /*#__PURE__*/_interopNamespaceDefault(iv);

const setterCoreContextMiddleware = (req, res, next) => {
  coreContext.setI18n(req.i18n);
  next();
};

const LOCALES = Object.values(frontendCountry_config.getCountriesData()).map(({
  locale
}) => locale.replace('_', '-')); // LOCALES always has values, so we can assert it as a non-empty array
function getLocaleFromAcceptLanguage(req) {
  const cookies = req.cookies;
  const acceptLang = cookies.locale || req.header('accept-language');
  return acceptLang && resolveAcceptLanguage.resolveAcceptLanguage(acceptLang, LOCALES, 'es-AR');
}
const i18nIVMiddleware = iv__namespace.createValidationMiddleware({
  schema: {
    query: iv__namespace.object({
      locale: iv__namespace.enumeration(LOCALES).optional()
    })
  }
});
const i18nLocaleMiddleware = (req, res, next) => {
  const locale = req.locale || req.query.locale || getLocaleFromAcceptLanguage(req);
  if (locale) {
    req.locale = locale;
  }
  return next();
};

/**
 * Creates an I18n instance per request, defining properties: `req.translations` and `req.i18n`.
 *
 * @see {@link https://nordic.adminml.com/docs/api/Nordic%20Modules/nordic-i18n#middlewares | Documentation}
 *
 * @example
 *
 * import ragnar from 'nordic/ragnar';
 * import { createNordicI18nMiddlewares } from 'nordic/i18n/middleware';
 *
 * const appRouter = router();
 *
 * appRouter.use(createNordicI18nMiddlewares());
 *
 */
function createNordicI18nMiddlewares(config = frontendConfig.config.i18n ?? {}) {
  const middlewares = [middleware.createI18nMiddleware({
    srcPath: config.srcPath,
    notifyMissingTranslations: config.notifyMissingTranslations,
    I18nClass: i18n.I18n
  }), setterCoreContextMiddleware];
  if (frontendConfig.config.intra) {
    middlewares.unshift(i18nIVMiddleware, i18nLocaleMiddleware);
  }
  return middlewares;
}

exports.createNordicI18nMiddlewares = createNordicI18nMiddlewares;
//# sourceMappingURL=server.development.js.map
