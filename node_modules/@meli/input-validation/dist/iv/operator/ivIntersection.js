"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const zod_1 = require("zod");
const ivObject_1 = require("../entity/ivObject");
const normalizeToZodSchemas_1 = __importDefault(require("./utils/normalizeToZodSchemas"));
function ivIntersection(left, right) {
    try {
        if (!(left instanceof ivObject_1.IvObject) && !(left instanceof zod_1.z.ZodType)) {
            throw new Error('Schema must be either an IvObject');
        }
        if (!(right instanceof ivObject_1.IvObject) && !(right instanceof zod_1.z.ZodType)) {
            throw new Error('Schema must be either an IvObject');
        }
        const toZodSchema = (0, normalizeToZodSchemas_1.default)([left, right]);
        const intersectionSchema = zod_1.z.intersection(toZodSchema[0], toZodSchema[1]);
        return intersectionSchema;
    }
    catch (error) {
        throw new Error(`Failed to create intersection: ${error instanceof Error ? error.message : String(error)}`);
    }
}
exports.default = ivIntersection;
