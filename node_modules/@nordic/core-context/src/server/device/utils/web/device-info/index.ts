import type { DeviceInfo } from '../../../types';

import { getDeviceModel } from './model';
import { getDeviceOS } from './os';

export function getDeviceInfo(): DeviceInfo {
  return {
    os: getDeviceOS(),
    model: getDeviceModel(),
  };
}
