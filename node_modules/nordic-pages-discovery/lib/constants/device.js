const SPECIFIC_DEVICE_TYPES = Object.freeze({
  desktop: 'desktop',
  mobile: 'mobile',
  tablet: 'tablet',
  webview: 'webview',
});

const DEVICE_TYPES = Object.freeze({
  ...SPECIFIC_DEVICE_TYPES,
  base: 'base',
});

module.exports = {
  DEVICE_TYPES,
  SPECIFIC_DEVICE_TYPES,
};
