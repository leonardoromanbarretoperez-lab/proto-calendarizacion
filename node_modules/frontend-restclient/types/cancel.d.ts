import type { AxiosStatic, CancelTokenStatic } from 'axios';

/**
 * @deprecated `CancelToken` is deprecated. Please use `AbortController` instead. [Documentation](https://github.com/axios/axios?tab=readme-ov-file#abortcontroller).
 * @example
 * const controller = new AbortController();
 *
 * client.get('/foo/bar', {
 *   signal: controller.signal
 * }).then((res) => {
 *   console.log(res);
 * });
 *
 * // cancel the request
 * controller.abort()
 */
export const CancelToken: CancelTokenStatic;

/**
 * @deprecated `isCancel` is deprecated. Please use `AbortController` instead. [Documentation](https://github.com/axios/axios?tab=readme-ov-file#abortcontroller).
 * @example
 * const controller = new AbortController();
 *
 * client.get('/foo/bar', {
 *   signal: controller.signal
 * }).then((res) => {
 *   console.log(res);
 * });
 *
 * // cancel the request
 * controller.abort()
 */
export const isCancel: AxiosStatic["isCancel"];
