type ViteDevServer = {
  transformRequest: (filePath: string) => Promise<{ map?: string } | null>;
};

export function getViteDevServer(): ViteDevServer | null {
  return {
    transformRequest: async () => Promise.resolve(null),
  };
}
