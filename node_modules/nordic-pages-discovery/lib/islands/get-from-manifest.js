const {
  readIslandsManifest: getAllIslands,
  getIslandByFullPath,
} = require('../helpers/file-system/islands-manifest');

module.exports = {
  /**
   * @returns {object} Discovered Islands
   */
  getAllIslands,
  getIslandByFullPath,
};
