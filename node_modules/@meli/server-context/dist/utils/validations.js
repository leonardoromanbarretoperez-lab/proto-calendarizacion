"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.validateModuleInstances = validateModuleInstances;
const node_child_process_1 = require("node:child_process");
const error_1 = require("./error");
const MODULE_NAME = '@meli/server-context';
function searchModuleInstances(dependenciesInfo, callback) {
    Object.entries(dependenciesInfo).forEach(([name, { version, dependencies }]) => {
        if (name === MODULE_NAME) {
            callback(version);
        }
        if (dependencies) {
            searchModuleInstances(dependencies, callback);
        }
    });
}
function validateModuleInstances() {
    const lsInfo = (0, node_child_process_1.execSync)(`npm ls ${MODULE_NAME} --json`, {
        encoding: 'utf8',
    });
    const contextInstances = new Set();
    const { dependencies } = JSON.parse(lsInfo);
    searchModuleInstances(dependencies, (version) => {
        contextInstances.add(`${MODULE_NAME}@${version}`);
    });
    if (contextInstances.size > 1) {
        throw new error_1.ServerContextError(`Multiple instances of ${MODULE_NAME} found: ${Array.from(contextInstances).join(', ')}.`);
    }
}
