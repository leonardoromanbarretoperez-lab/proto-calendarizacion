import type { ZodTypeAny } from 'zod';
import { IvObject } from '../iv/entity/ivObject';
export type GetZodSchema<S extends IvObject<any> | ZodTypeAny> = S extends IvObject<any> ? ReturnType<S['getSchema']> : S;
/**
 * Resolves a schema from IvObject or ZodTypeAny to its underlying Zod schema.
 * Throws an error if the schema is null, undefined, or not a valid type.
 */
export declare function resolveSchema<S extends IvObject<any> | ZodTypeAny>(schema: S, entityName?: string): GetZodSchema<S>;
