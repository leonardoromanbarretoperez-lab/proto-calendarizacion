/**
 * Module dependencies
 */
const Logger = require('./Logger');

const LoggerFactory = (name, options) => new Logger(name, options);

/**
 * Expose logger factory
 */
module.exports = LoggerFactory;
exports = module.exports;
exports.LoggerFactory = LoggerFactory;
