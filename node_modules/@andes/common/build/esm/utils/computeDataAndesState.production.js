import _slicedToArray from '@babel/runtime/helpers/slicedToArray';
const computeDataAndesState = stateObject => {
  const datasetValue = Object.entries(stateObject).reduce((acc, _ref) => {
    let _ref2 = _slicedToArray(_ref, 2),
      key = _ref2[0],
      value = _ref2[1];
    if (key === 'otherDataAndesState') {
      const otherStates = value.split(' ');
      otherStates.forEach(state => {
        acc.push(state);
      });
    } else if (value) {
      acc.push(key);
    }
    return acc;
  }, []);
  return {
    'data-andes-state': datasetValue.join(' ') || undefined
  };
};
export { computeDataAndesState };
