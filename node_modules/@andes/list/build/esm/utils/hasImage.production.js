import { isValidElement } from 'react';
const hasImage = leftContent => {
  var _leftContent$type;
  const isThumbnail = /*#__PURE__*/isValidElement(leftContent) && (leftContent === null || leftContent === void 0 || (_leftContent$type = leftContent.type) === null || _leftContent$type === void 0 ? void 0 : _leftContent$type.displayName) === 'Thumbnail';
  const isImage = typeof (leftContent === null || leftContent === void 0 ? void 0 : leftContent.src) === 'string';
  return !!(isThumbnail || isImage);
};
export { hasImage };
