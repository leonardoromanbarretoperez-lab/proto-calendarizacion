const ids = {
  PRODUCTION: 'production',
  TEST: 'test',
};

const environmentConstants = {
  [ids.PRODUCTION]: {
    endpoint: '/production/common.json',
    suffix: '',
  },
  [ids.TEST]: {
    endpoint: '/test/common.json',
    suffix: 'test',
  },
};

const testEnvironmentLab = 'develop-nordic';

module.exports = {
  ids,
  environmentConstants,
  testEnvironmentLab,
};
