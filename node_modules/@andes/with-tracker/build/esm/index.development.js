import _objectWithoutProperties from '@babel/runtime/helpers/objectWithoutProperties';
import _objectSpread from '@babel/runtime/helpers/objectSpread2';
import _extends from '@babel/runtime/helpers/extends';
import React from 'react';
const _excluded = ["ref"];
const isSSR = () => typeof window === 'undefined';

/** @type {typeof import('../..').withTracker<any>} */
const withTracker = function (WrappedComponent) {
  let metrics = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : {};
  class TrackerHOC extends React.PureComponent {
    /** @param {typeof import('../..').WithTrackerProps} props */
    constructor(props) {
      super(props);
      if (isSSR()) {
        if ('SSR' in metrics && Array.isArray(metrics.SSR)) {
          metrics.SSR.forEach(/** @type {() => void} */track => {
            if (typeof track === 'function') track(props);
          });
        }
      }
    }
    render() {
      const forwardedRef = this.props.forwardedRef;
      return /*#__PURE__*/React.createElement(WrappedComponent, _extends({}, this.props, {
        ref: forwardedRef
      }));
    }
  }
  TrackerHOC.propTypes = _objectSpread({}, WrappedComponent.propTypes);

  // @ts-expect-error ref could be of any type
  const Component = _ref => {
    let ref = _ref.ref,
      props = _objectWithoutProperties(_ref, _excluded);
    return /*#__PURE__*/React.createElement(TrackerHOC, _extends({}, props, {
      forwardedRef: ref
    }));
  };
  Component.displayName = WrappedComponent.displayName;
  return Component;
};
var withTracker$1 = withTracker;
export { withTracker$1 as withTracker };
//# sourceMappingURL=index.development.js.map
