import { RequestHandler } from 'express';

declare function getCoreContextInitScript(staticMarkup?: boolean): {
    id: string;
    children: string;
};

declare const coreContextMiddleware: RequestHandler;
declare const webviewCookiesMiddleware: RequestHandler;

declare function getLayoutMiddlewareOptions<T = unknown>(): T | undefined;

declare function setLayoutMiddlewareOptions<T = unknown>(layoutMiddlewareOptions: T): void;

export { coreContextMiddleware, getCoreContextInitScript, getLayoutMiddlewareOptions, setLayoutMiddlewareOptions, webviewCookiesMiddleware };
