"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.json2tags = json2tags;
exports.buildLine = buildLine;
exports.createPlainTextStream = createPlainTextStream;
const pino_abstract_transport_1 = __importDefault(require("pino-abstract-transport"));
const PINO_INTERNAL_FIELDS = new Set([
    'level',
    'time',
    'msg',
    'pid',
    'hostname',
]);
/**
 * Convert a tags object to the `[key:value] [key:value]` format.
 */
function json2tags(tags) {
    let result = '';
    Object.keys(tags).forEach((key) => {
        // eslint-disable-next-line @typescript-eslint/restrict-template-expressions
        result += ` [${key}:${tags[key]}]`;
    });
    return result.substring(1);
}
/**
 * Build a plain-text log line from a parsed pino log object.
 * Format: `timestamp - level: msg - [tag:value]... [level:LEVEL]`
 */
function buildLine(pinoLog) {
    const { time, level, msg } = pinoLog;
    const tags = {};
    Object.keys(pinoLog).forEach((key) => {
        if (!PINO_INTERNAL_FIELDS.has(key)) {
            tags[key] = pinoLog[key];
        }
    });
    tags.level = level;
    const tagsStr = json2tags(tags);
    return `${time} - ${level.toLowerCase()}: ${msg} - ${tagsStr}`;
}
/**
 * Creates a stream that formats pino log objects as plain text and writes them to stdout.
 * Uses pino-abstract-transport to receive pre-parsed objects, avoiding JSON.parse overhead.
 */
function createPlainTextStream() {
    return (0, pino_abstract_transport_1.default)(async (source) => {
        for await (const obj of source) {
            process.stdout.write(`${buildLine(obj)}\n`);
        }
    });
}
