"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
/* eslint-disable @typescript-eslint/no-explicit-any */
const PropertyDisabler_1 = __importDefault(require("./PropertyDisabler"));
class ModifierChaining {
    static applyModifiers(instance) {
        const modifiers = ['optional', 'nullable', 'nullish'];
        modifiers.forEach((modifier) => {
            const originalMethod = Object.getPrototypeOf(instance)[modifier];
            if (originalMethod) {
                instance[modifier] = () => {
                    const result = originalMethod.call(instance);
                    PropertyDisabler_1.default.disableProperties(result);
                    return result;
                };
            }
        });
        const originalOr = Object.getPrototypeOf(instance).or;
        if (originalOr) {
            instance.or = (right) => {
                const result = originalOr.call(instance, right);
                PropertyDisabler_1.default.disableProperties(result);
                return result;
            };
        }
        const originalAnd = Object.getPrototypeOf(instance).and;
        if (originalAnd) {
            instance.and = (right) => {
                const result = originalAnd.call(instance, right);
                PropertyDisabler_1.default.disableProperties(result);
                return result;
            };
        }
    }
    static applyArrayModifiers(instance) {
        const arrayModifiers = ['min', 'max', 'length', 'nonempty'];
        arrayModifiers.forEach((modifier) => {
            const originalMethod = Object.getPrototypeOf(instance)[modifier];
            if (originalMethod) {
                instance[modifier] = (...args) => {
                    const result = originalMethod.apply(instance, args);
                    PropertyDisabler_1.default.disableProperties(result);
                    ModifierChaining.applyModifiers(result);
                    return result;
                };
            }
        });
    }
}
exports.default = ModifierChaining;
