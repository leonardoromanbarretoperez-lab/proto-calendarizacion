"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const zod_1 = require("zod");
const ivObject_1 = require("../entity/ivObject");
const normalizeToZodSchemas_1 = __importDefault(require("./utils/normalizeToZodSchemas"));
function ivDiscriminatedUnion(discriminator, schemas) {
    if (!discriminator) {
        throw new Error('Discriminator must be a non-empty string');
    }
    try {
        const schemasArray = schemas;
        // Validación para mantener compatibilidad con tests
        if (!Array.isArray(schemasArray)) {
            throw new Error('Schema must be either an IvObject');
        }
        if (schemasArray.length < 2) {
            throw new Error('At least two schemas are required');
        }
        const invalidSchemas = schemasArray.filter((schema) => !(schema instanceof ivObject_1.IvObject) && !(schema instanceof zod_1.z.ZodType));
        if (invalidSchemas.length > 0) {
            throw new Error('Schema must be either an IvObject');
        }
        const discriminatedUnionSchema = zod_1.z.discriminatedUnion(discriminator, (0, normalizeToZodSchemas_1.default)(schemasArray));
        return discriminatedUnionSchema;
    }
    catch (error) {
        throw new Error(`Failed to create discriminated union: ${error instanceof Error ? error.message : String(error)}`);
    }
}
exports.default = ivDiscriminatedUnion;
