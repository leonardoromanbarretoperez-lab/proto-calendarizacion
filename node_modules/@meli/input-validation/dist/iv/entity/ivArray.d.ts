import type { ZodTypeAny } from 'zod';
import type { IvObject, ValidateOptions } from './ivObject';
import type { GetZodSchema } from '../../utils/types';
import { z } from 'zod';
export interface IvArrayOptions {
    exactLength?: number | null;
    minLength?: number | null;
    maxLength?: number | null;
}
declare class IvArray<T extends ZodTypeAny> extends z.ZodArray<T> {
    constructor(schema: T, options?: IvArrayOptions);
    validate(input: unknown[], options?: ValidateOptions): boolean;
}
declare const ivArray: <S extends IvObject<any> | ZodTypeAny>(schema: S, options?: IvArrayOptions) => IvArray<GetZodSchema<S>>;
export default ivArray;
export { IvArray };
