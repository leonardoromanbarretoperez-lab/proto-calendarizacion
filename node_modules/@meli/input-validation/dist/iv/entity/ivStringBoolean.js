"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const zod_1 = require("zod");
const secureTypeMetric_1 = require("../../utils/secureTypeMetric");
/**
 * Parses string-encoded booleans from HTTP query/path params.
 *
 * Unlike `iv.coerce.boolean()` (which uses JavaScript's `Boolean()` and coerces
 * any non-empty string — including `'false'` — to `true`), this method applies
 * strict semantic mapping:
 *
 * - `'true'`  → `true`
 * - `'false'` → `false`
 * - anything else → validation error
 *
 * @example
 * // ?active=true&verified=false
 * const schema = iv.object({
 *   active: iv.stringBoolean(),
 *   verified: iv.stringBoolean(),
 * });
 */
const parseStringBoolean = (v) => {
    if (v === 'true') {
        return true;
    }
    if (v === 'false') {
        return false;
    }
    return v;
};
const ivStringBoolean = (0, secureTypeMetric_1.createSecureType)(() => zod_1.z.preprocess(parseStringBoolean, zod_1.z.boolean()));
exports.default = ivStringBoolean;
