import type { ZodDiscriminatedUnionOption } from 'zod';
import { z } from 'zod';
import { IvObject } from '../entity/ivObject';
declare function ivDiscriminatedUnion<D extends string, T extends [
    ZodDiscriminatedUnionOption<D>,
    ...Array<ZodDiscriminatedUnionOption<D>>
]>(discriminator: D, schemas: {
    [K in keyof T]: IvObject<T[K]> | T[K];
}): z.ZodDiscriminatedUnion<D, T>;
export default ivDiscriminatedUnion;
