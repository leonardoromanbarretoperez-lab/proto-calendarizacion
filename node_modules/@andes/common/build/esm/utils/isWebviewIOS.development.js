const IOS_OS = 'ios';

// nativeApp is only available in a Webview context
/** @param {{[key: string]: any}} device */
const isWebviewIOS = device => {
  var _device$nativeApp;
  return ((device === null || device === void 0 || (_device$nativeApp = device.nativeApp) === null || _device$nativeApp === void 0 ? void 0 : _device$nativeApp.os) || '').toLowerCase() === IOS_OS;
};
export { isWebviewIOS as default };
//# sourceMappingURL=isWebviewIOS.development.js.map
