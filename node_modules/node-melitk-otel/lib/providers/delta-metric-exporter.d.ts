import { AggregationTemporality, type InstrumentType } from '@opentelemetry/sdk-metrics';
import { OTLPMetricExporter } from '@opentelemetry/exporter-metrics-otlp-grpc';
/**
 * Custom metric exporter that uses DELTA temporality for all instrument types by default
 * (matches Datadog requirements). Can be overridden via
 * OTEL_EXPORTER_OTLP_METRICS_TEMPORALITY_PREFERENCE environment variable.
 */
export declare class DeltaMetricExporter extends OTLPMetricExporter {
    selectAggregationTemporality(instrumentType: InstrumentType): AggregationTemporality;
}
