import type { RequestHandler } from 'express';

import { setI18n } from '@nordic/core-context';

export const setterCoreContextMiddleware: RequestHandler = (req, res, next) => {
  setI18n(req.i18n);
  next();
};
