"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const zod_1 = require("zod");
const normalizeToZodSchemas_1 = __importDefault(require("./utils/normalizeToZodSchemas"));
function ivUnion(schemas) {
    try {
        const toZodSchema = (0, normalizeToZodSchemas_1.default)(schemas);
        const unionSchema = zod_1.z.union(toZodSchema);
        return unionSchema;
    }
    catch (error) {
        throw new Error(`Failed to create union: ${error instanceof Error ? error.message : String(error)}`);
    }
}
exports.default = ivUnion;
