# @andes/listbox

ListBox presents a list of options and allows a user to select one or more of them.
> This is an internal component, if you need to use it please contact us to follow the use case.

## How to use

### Installation

> Follow the [general installation guide](../../docs/installation.md).

```bash
$ npm install --save @andes/listbox
```

### Styles

> Follow the [general styles usage guide](../../docs/styles-usage.md) for configuration and how-tos.

Import the component in your bundle:

```scss
@import '@andes/listbox/index';
```

### Scripts

> Follow the [general scripts usage guide](../../docs/scripts-usage.md) for configuration and how-tos.

Import the component in your page:

```jsx
import { Listbox } from '@andes/listbox';
```

## Examples

For full demo and configuration options, check out the [story for Listbox](https://andes.adminml.com/web/?path=/story/listbox--andes-list-box).

It's recommended to use [`RadioList`](https://github.com/melisource/fury_frontend-andes-ui/tree/master/packages/radio-list) or [`CheckboxList`](https://github.com/melisource/fury_frontend-andes-ui/tree/master/packages/checkbox-list) for single or multiple selection, respectively.

```jsx
<ListBox defaultSelectedKeys={['2']} selectionMode="multiple" id="selectable-example" srLabel="Selectable List">
  <ListItem value="0" title="DHL Express" description="Arrives May 10th" rightContent="Free"/>
  <ListItem value="1" title="DHL Express" description="Arrives May 12nd" disabled />
  <ListItem value="2" title="DHL Premium" />
</ListBox>
```

### Shadow DOM 

Usage inside an isolated component with Shadow DOM. See [@andes/shadow-dom](https://github.com/melisource/fury_frontend-andes-ui/blob/master/packages/shadow-dom/README.md) package

```jsx
import { useEnableAndesShadowDOM } from '@andes/shadow-dom';

useEnableAndesShadowDOM();

<ListBox defaultSelectedKeys={['2']} selectionMode="multiple" id="selectable-example" srLabel="Selectable List">
  <ListItem value="0" title="DHL Express" description="Arrives May 10th" rightContent="Free"/>
  <ListItem value="1" title="DHL Express" description="Arrives May 12nd" disabled />
  <ListItem value="2" title="DHL Premium" />
</ListBox>
```

## Props
| Property | Description | Type | Default | Required |
| -------- | ----------- | ---- | ------- | -------- |
| `children` | `<ListItem>` / `<ListGroup>` | `node` | `null`  | ✅ |
| `className` | Custom class. | `string` | `''` |  |
| `defaultSelectedKeys` | Selectable list's initial set of values (uncontrolled behavior). | `array` | `undefined` |  |
| `id` | Selectable list unique Id, used for a11y improvements. | `string` | `undefined` | ✅ |
| `isRadioList` | Determines if the listbox is radio list or not. | `boolean` | `false` |  |
| `onClick` | Function to execute when an option is clicked. | `function: (event, itemValue) => {}` | `undefined` |  |
| `onSelect` | Function to execute when an option is selected/unselected. | `function: (event, selectedValues, itemDetails) => {}` | `undefined` |  |
| `selectedKeys` | Selectable list's initial set of values (controlled behavior). | `array` | `undefined` |  |
| `selectionMode` | Allows single or multiple item selection. | `string: 'single', 'multiple'` | `single` |  |
| `selectionComponent` | Custom element that will act as a selector in the list. | `element` | `undefined` |  |
| `shouldUseVirtualFocus` | Whether the listbox items should use virtual focus instead of being focused directly. | `bool` | `undefined` |  |
| `srLabel` | <sup>1</sup>ListBox label for screen readers. | `string` | `undefined` | |

> 1- `srLabel` should be provided for accessibility reasons.

## Theming

This component supports theming. It allows you to pass a `customTheme` object to it adjusting colors, typography, spacing and styles to align with a brand's visual identity, user preferences or project requirements.

<br />

The `customTheme` object can make use of values ​​referenced in the component or CSS properties for quick customization or make use of style overrides according to `data-attributes` specific to the component for more complex cases. We do not recommend the latter since it will be deprecated in the future.

<br />

### Using Referenced Component Values

The ListBox allows you to modify the following attributes depending on its reference value.

| Sub Attribute | Description           |
| ------------- | --------------------- |
| `border`      | List border color.    |
| `fill`        | List fill color.      |
| `icon`        | List icon color.      |
| `text`        | List text color.      |

```jsx
const customTheme = {
  components: {
    list: {
      color: {
        fill: {
          [status]: fillColorValue
        },
        text: {
          [status]: baseColor,
          description: {
            [status]: baseColor,
          }
        },
        icon: {
          [position]: iconColorValue
        },
        border: borderColor,
      },
      pickList: {
        color: {
          selected: {
            text: {
              [status]: fillColorValue
            },
            icon: {
              [status]: fillColorValue
            },
            pipe: {
              [status]: fillColorValue
            },
          },
        },
      },
      group: {
        header: {
          color: {
            fill: fillColorValue,
            text: fillColorValue,
            border: fillColorValue,
          },
        },
      },
    },
  },
};
```

Possible `status` values:

```jsx

state: default | hover | active | disabled


```

Possible `position` values:

```jsx

position: left | right


```

> More states may be added in the future, so we recommend consulting the @andes/themes/meli-themes/ml.json file for more detailed information

#### Example

<img src="/resources/listbox/docs/ListBox-Example-Reference-Theming-Default.png" alt="Reference Theming List Example" width="720" />

```jsx
import React from 'react';
import { ListBox} from '@andes/listbox';
import { ListItem } from '@andes/list';
import { AndesThemeProvider } from '@andes/themes';
import Camera20 from '@andes/icons/Camera20';
import './styles.scss';

const customTheme = {
  components: {
    list: {
      color: {
        fill: {
          default: '#ECF2FF',
        },
        title: {
          default: '#487cfc',
        },
        description: {
          default: '#487cfc',
        },
        icon: {
          left: '#487cfc',
          right: '#487cfc',
        },
        border: '#C4D5FF',
      },
      pickList: {
        color: {
          selected: {
            title: {
              default: '#2a66fa',
            },
            icon: {
              default: '#2a66fa',
            },
            pipe: {
              default: '#2a66fa',
            },
          },
        },
      },
    },
  },
};

const MyComponent = () => (
  <AndesThemeProvider theme={customTheme}>
    <ListBox srLabel="Product List" withDividers defaultSelectedKeys={['1']}>
      <ListItem
        leftContent={<Camera20 />}
        title="Row title"
        description="Row description"
        value="1"
      />
      <ListItem
        leftContent={<Camera20 />}
        title="Row title"
        description="Row description"
        value="2"
      />
    </ListBox>
  </AndesThemeProvider>
);

export default MyComponent;
```

```css
/* styles.scss */
@import '@andes/listbox/index';
```

> Remember that it is not necessary for the ThemeProvider to be next to the component you want to customize. Ideally it should be at the App level, wrapping your entire page.

### Using CSS Properties

The component allows passing CSS properties following this structure:

```jsx
const customTheme = {
  components: {
    list: {
      [cssProperty]: propertyValue,
    },
  },
};
```

> Please note that styles applied in this way will impact all Andes switches regardless of their variant. Additionally, they have higher precedence than styles applied using referenced component values.

#### Example

<img src="/resources/listbox/docs/ListBox-Example-CSS-Properties-Theming.png" alt="CSS Properties Theming List Example" width="720" />


```jsx
import React from 'react';
import { ListBox } from '@andes/listbox';
import { ListItem } from '@andes/list';
import { AndesThemeProvider } from '@andes/themes';
import './styles.scss';

const customTheme = {
  components: {
    list: {
      padding: '0 16px',
      borderRadius: '8px',
      boxShadow: '0 10px 15px -3px rgb(0 0 0 / 0.1), 0 4px 6px -4px rgb(0 0 0 / 0.1)',
    },
  },
};

const MyComponent = () => (
  <AndesThemeProvider theme={customTheme}>
    <ListBox srLabel="Product List" withDividers>
      <ListItem title="Row title" paddingSize={16} value="0" />
      <ListItem title="Row title" paddingSize={16} value="1" />
      <ListItem title="Row title" paddingSize={16} value="2" />
      <ListItem title="Row title" paddingSize={16} value="3" />
    </ListBox>
  </AndesThemeProvider>
);

export default MyComponent;
```

```css
/* styles.scss */
@import '@andes/listbox/index';
```

> Remember that it is not necessary for the ThemeProvider to be next to the component you want to customize. Ideally it should be at the App level, wrapping your entire page.

### Using `styleOverrides`

This component has a series of `data-attributes` that serve as a hook to style complex cases:

| data-attribute                       | Description                                                                          |
| ------------------------------------ | ------------------------------------------------------------------------------------ |
| `data-andes-listbox`                | List container.                                                                  |
| `data-andes-list-group`          | List group container.                                                                  |
| `data-andes-list-group-header`  | List group header.                                                                     |
| `data-andes-list-group-sublist` | List group sublist.                                                                    |
| `data-andes-listbox-item`           | List item container.                                                                  |
| `data-andes-listbox-item-size`      | List sizes. Can be: `'small'`,
`'medium'`, `'large'`                                                       |
| `data-andes-listbox-title`          | List title.                                                                      |
| `data-andes-listbox-description`    | List description.                                                                |
| `data-andes-listbox-left-content`   | List left content.                                                                    |
| `data-andes-listbox-right-content`  | List right content.                                                                    |
| `data-andes-listbox-checkbox`       | Checkbox                                                                    |
| `data-andes-state`               | List state. Can be: `'disabled'` or
`'selected'`.                                                               |

Taking advantage of the them, you can use an entry called `styleOverrides` where you can add styles according to any `data-attribute`.

#### Example

<img src="/resources/listbox/docs/ListBox-Example-Style-Overrides-Theming.png" alt="Style Overrides Theming List Example" width="720" />


```jsx
import React from 'react';
import { ListBox } from '@andes/listbox';
import { ListItem } from '@andes/list';
import { AndesThemeProvider } from '@andes/themes';
import './styles.scss';

const customTheme = {
  components: {
    list: {
      styleOverrides: {
        '&[data-andes-listbox]': {
          borderRadius: '8px',
          overflow: 'hidden',
        },
        '[data-andes-listbox-item]': {
          backgroundColor: '#ECF2FF',
          borderColor: '#C4D5FF',
        },
        '[data-andes-listbox-item-size="small"]': {
          height: '48px',
        },
        '[data-andes-listbox-item-size="medium"]': {
          height: '56px',
        },
        '[data-andes-listbox-item-size="large"]': {
          height: '64px',
        },
      },
    },
  },
};

const MyComponent = () => (
  <AndesThemeProvider theme={customTheme}>
    <ListBox srLabel="Product List" withDividers>
      <ListItem title="Row title" size="small" value="0" />
      <ListItem title="Row title" size="medium" value="1" />
      <ListItem title="Row title" size="large" value="2" />
    </ListBox>
  </AndesThemeProvider>
);

export default MyComponent;
```

```css
/* styles.scss */
@import '@andes/listbox/index';
```

> Remember that it is not necessary for the ThemeProvider to be next to the component you want to customize. Ideally it should be at the App level, wrapping your entire page.

## Slots

This component supports the use of slots, providing flexible content areas that can be occupied by other components, thus facilitating the composition of more complex structures. Although React does not have a specific feature called "slots," similar behavior can be achieved using props.

<br />

In Andes, we will have default slots, referred to as children, and named slots that depend on each specific component. This means we can define areas where custom content can be inserted, enhancing the flexibility and reusability of our components.

<br />

### Use of slots

The ListItem allows you to modify the following slots.

| Slot name            | Description                                               |
| -------------------- | --------------------------------------------------------- |
| `children (default)` | Allows for custom content in list rows. This slot is ideal for situations where the default list design does not meet the specific needs of the interface. |

```jsx
<ListBox srLabel="Product List">
  <ListItem value="0">
    Content of the Row Slot
  </ListItem>
</ListBox>
```

#### Example

<img src="resources/listbox/docs/ListBox-Example-Reference-Slot.png" alt="Example of Slots in a ListBox" width="1062" />

```jsx
/* CustomLayout.js */
import React from 'react';
import PlaceholderImage16 from '@andes/icons/PlaceholderImage16';
import { ThumbnailMultiple, Thumbnail } from '@andes/thumbnail';
import { Text } from '@andes/typography';
import './customLayout.scss';

const CustomLayout = () => (
  <div className="layout">
    <div className="layout__first-column" aria-hidden>
      <ThumbnailMultiple
        modifier="square"
        containerSize="72"
        type="grid"
        overflow={6}
      >
        <Thumbnail>
          <img
            alt=""
            src="https://http2.mlstatic.com/D_Q_NP_221705-MLA25074486775_092016-I.webp"
          />
        </Thumbnail>
        <Thumbnail>
          <img
            alt=""
            src="https://http2.mlstatic.com/D_Q_NP_221705-MLA25074486775_092016-I.webp"
          />
        </Thumbnail>
        <Thumbnail>
          <img
            alt=""
            src="https://http2.mlstatic.com/D_Q_NP_221705-MLA25074486775_092016-I.webp"
          />
        </Thumbnail>
      </ThumbnailMultiple>
    </div>

    <div className="layout__second-column">
      <div className="custom-content">
        <Text size="l" weight="semibold">Gaming</Text>
        <div className="custom-content__text-icon">
          <PlaceholderImage16 />
          <Text size="xs">Lista de deseos</Text>
        </div>
        <Text size="xs" color="secondary">Privada</Text>
      </div>
      <Text size="xs" color="secondary">3 productos</Text>
    </div>
  </div>
);

export default CustomLayout;
```

```css
/* customLayout.scss */
@import '@andes/thumbnail/index';
@import '@andes/typography/index';

.layout {
  display: grid;
  grid-template-columns: auto 1fr auto;
  grid-gap: 8px;
  justify-content: start;
  padding-top: $andes-spacing-20;
  padding-bottom: $andes-spacing-20;
}

.layout__second-column {
  display: flex;
  flex-direction: column;
  gap: $andes-spacing-8;
}

.custom-content {
  display: flex;
  flex-direction: column;
  gap: $andes-spacing-4;
}

.custom-content__text-icon {
  display: flex;
  gap: $andes-spacing-4;
}
```

```jsx
/* App.js */
import React from 'react';
import { ListItem } from '@andes/list';
import { ListBox } from '@andes/listbox';
import CustomLayout from './CustomLayout';
import './styles.scss';

const App = () => {
  return (
    <ListBox srLabel="Product List">
      <ListItem value="0">
        <CustomLayout />
      </ListItem>
    </ListBox>
  )
}
```

```css
/* styles.scss */
@import '@andes/listbox/index';
```